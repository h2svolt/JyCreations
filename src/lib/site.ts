/** Canonical site origin — the domain everything redirects to (jycreations.store
 * apex 308s here). Used for canonical links, og:url, structured data, and the
 * sitemap, so they never drift out of sync with what Google actually crawls. */
export const SITE_URL = "https://www.jycreations.store";
